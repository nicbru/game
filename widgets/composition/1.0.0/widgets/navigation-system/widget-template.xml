<container position="{@position}" float="{@float}" offset="{@offset}" width="{@width}" height="{@height}" left="{@left}" right="{@right}" top="{@top}" bottom="{@bottom}" margin="{@margin}" margin-top="{@margin-top}" margin-bottom="{@margin-bottom}" margin-left="{@margin-left}" margin-right="{@margin-right}" padding="{@padding}" padding-top="{@padding-top}" padding-bottom="{@padding-bottom}" padding-left="{@padding-left}" padding-right="{@padding-right}" min-width="{@min-width}" max-width="{@max-width}" min-height="{@min-height}" max-height="{@max-height}" visible="{@visible}" overflow="{@overflow}" class="{@class}">
	
</container>
